apiVersion: apps/v1
kind: Deployment
metadata:
  name: shipping-service
  namespace: eda-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shipping-service
  template:
    metadata:
      labels:
        app: shipping-service
    spec:
      containers:
        - name: shipping-service
          image: ghcr.io/example/shipping-service:latest
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: eda-platform
spec:
  selector:
    app: shipping-service
  ports:
    - port: 80
      targetPort: 8080

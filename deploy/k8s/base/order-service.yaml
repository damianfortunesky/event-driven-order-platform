apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: eda-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - name: order-service
          image: ghcr.io/example/order-service:latest
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: eda-platform
spec:
  selector:
    app: order-service
  ports:
    - port: 80
      targetPort: 8080

apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: eda-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
        - name: payment-service
          image: ghcr.io/example/payment-service:latest
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: eda-platform
spec:
  selector:
    app: payment-service
  ports:
    - port: 80
      targetPort: 8080
